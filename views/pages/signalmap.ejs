<html lang="en">

<!--main-->
  <%- contentFor('signalmap') %>

   <main id="sitemain">    
        <div class="hero">
            <%= slist[0].id %>
        </div>
   </main>  
  
    <div id="map"></div>
   
   <script>

      function initMap(signalcoord) {
        var bengaluru = {lat: 12.9427, lng: 77.5858};
       // var signalloc = (lat: signalcoord.lat, lng: signalcoord.long)
        var map = new google.maps.Map(document.getElementById('map'), {
          zoom: 12,
          center: bengaluru
        });
        var marker = new google.maps.Marker({
          position: bengaluru,
          map: map
        });
      }
    //  initMap(slist);
      //for(let i=0;i <= len ; i++)
        //  initMap(slist[i]);
    </script>
    <script async defer
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC8VpZvaDz8AyqC_JV73XlAMEV48HTHAmA&callback=initMap">
    </script>